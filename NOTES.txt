(Done) Make a brief description of each module in plain English of each module

(Done) data preprocessing name -> cleaning

(Done) fix keras error ->
    The reason here is that tensorflow tries to load the keras module in a lazy fashion, which means that it holds only
    a reference to the module until the module is used. Only then the keras module will be actually loaded. Therefore
    IDEs only know about the reference tensorflow holds to the keras module and not its content.
    https://stackoverflow.com/questions/71000250/import-tensorflow-keras-could-not-be-resolved-after-upgrading-to-tensorflow-2

Once you finished with Pycharm, make the entire project runnnable as python applicaion.
Investigate a bit about "setup.py" files-technology to do final deliverables.

If you feel confident, would try a simple GUI for this? You only Tkinter.